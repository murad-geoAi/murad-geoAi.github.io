# Build-time only. The website itself ships no dependencies — these are needed
# solely to regenerate demos/spatial-cv-demo.html, whose output is committed.
#
#   py -m pip install -r scripts/requirements.txt
#   py scripts/make_demo_map.py

numpy>=1.24
scikit-learn>=1.3
folium>=0.15
